# 开发日志 - DEV-024 - 配置文件优化

**开发日期**: 2026-05-27  
**开发阶段**: Phase 24  
**开发状态**: ✅ 已完成

---

## 📋 开发目标

1. 修改打开锻造站的指令为 `/icf gui <forge-id>`
2. 将消息配置从 `config.yml` 分离到独立的 `messages.yml`
3. 清理 forges 配置文件夹：
   - 删除旧的示例模板.yml
   - 将铁匠铺.yml 重命名为 example_template.yml（使用英文）
4. 配置文件中的注释全部改为英文

---

## 🔧 修改内容

### 1. 命令修改

**文件**: `command/ForgeCommand.java`

#### 修改前

```java
switch (subCommand) {
    case "reload" -> { ... }
    case "list" -> { ... }
    default -> {
        // 直接使用第一个参数作为 forge-id
        openForge(player, subCommand);
    }
}

private void sendHelp(CommandSender sender) {
    sender.sendMessage("§6=== ItemCoreForge 帮助 ===");
    sender.sendMessage("§e/icf <锻造台ID> §7- 打开指定锻造台");
    sender.sendMessage("§e/icf list §7- 列出所有锻造台");
    sender.sendMessage("§e/icf reload §7- 重载配置");
}
```

#### 修改后

```java
switch (subCommand) {
    case "reload" -> { ... }
    case "list" -> { ... }
    case "gui" -> {
        // 需要第二个参数作为 forge-id
        if (args.length < 2) {
            sendHelp(sender);
            return true;
        }
        openForge(player, args[1]);
    }
    default -> {
        // 未知子命令，显示帮助
        sendHelp(sender);
    }
}

private void sendHelp(CommandSender sender) {
    sender.sendMessage("§6=== ItemCoreForge Help ===");
    sender.sendMessage("§e/icf gui <forge-id> §7- Open specified forge");
    sender.sendMessage("§e/icf list §7- List all forges");
    sender.sendMessage("§e/icf reload §7- Reload configs");
}
```

### 2. 新建 messages.yml

**文件**: `resources/messages.yml`

```yaml
# ================================================
# ItemCoreForge Messages Configuration
# ================================================

# Message prefix
prefix: "&8[&6Forge&8] &7"

# General messages
forge-not-found: "&cForge not found: &e{forge}"
recipe-not-found: "&cRecipe not found"
no-permission: "&cYou do not have permission"
reloaded: "&aForge configurations reloaded!"

# Crafting messages
crafting-started: "&7Started crafting &e{recipe}&7, please wait {time} seconds..."
crafting-complete: "&aCrafting complete! &e{result} &ais ready to claim"
crafting-cancelled: "&aCrafting cancelled: &e{recipe}"
item-claimed: "&aClaimed: &e{result}"

# Material/condition messages
materials-insufficient: "&cInsufficient materials!"
conditions-not-met: "&cConditions not met!"
consume-on-fail: "&cCrafting failed! Materials consumed"

# Queue messages
queue-full: "&cCrafting queue is full, please wait"
in-queue: "&7Already in queue, waiting to craft..."
queue-info: "&aCurrently crafting: &e{recipe}&a, remaining: &e{time}s"
queue-waiting: "&7Waiting in queue: &e{count} &7items"

# Error messages
inventory-full: "&cInventory full! Please clear your inventory first"
backpack-full-cancel: "&cInventory full! Please clear your inventory before cancelling"
backpack-full-claim: "&cInventory full! Please clear your inventory before claiming"

# Help messages
help-header: "&6=== ItemCoreForge Help ==="
help-gui: "&e/icf gui <forge-id> &7- Open specified forge"
help-list: "&e/icf list &7- List all forges"
help-reload: "&e/icf reload &7- Reload configurations"

# List messages
list-header: "&6=== Forge List ==="
list-item: "&7- &e{forge} &8(Recipes: {count})"
```

### 3. 修改 config.yml

**文件**: `resources/config.yml`

#### 改动

1. 移除了 `messages` 部分（移到 messages.yml）
2. 所有注释改为英文
3. 更新了默认值的显示文本（如 `craft-item-name`）

```yaml
# ================================================
# ItemCoreForge Main Configuration
# ================================================

# Basic settings
settings:
  forges-directory: "forges"
  layouts-directory: "layouts"
  player-data-directory: "player-data"
  default-success-rate: 100
  consume-on-fail: true
  show-messages: true
  log-crafts: true

# GUI Settings
gui:
  recipes-per-page: 28
  border-item: GRAY_STAINED_GLASS_PANE
  previous-item: ARROW
  next-item: ARROW
  back-item: BARRIER
  craft-item: GREEN_WOOL
  craft-item-name: "&aStart Crafting"  # 从中文改为英文
  craft-item-lore:
    - "&7Click to start crafting"
    - "&7Craft Time: {craft_time}s"
  craft-disabled-item: RED_WOOL
  craft-disabled-name: "&cInsufficient Materials"  # 从中文改为英文
  craft-disabled-lore:
    - "&7Insufficient materials"
    - "&7Please collect materials first"
```

### 4. 修改 ConfigManager

**文件**: `config/ConfigManager.java`

#### 新增内容

```java
public class ConfigManager {
    private FileConfiguration messages;  // 新增消息配置

    public void load() {
        plugin.saveDefaultConfig();
        config = plugin.getConfig();
        createDefaultDirectories();
        createDefaultFiles();
        loadMessages();  // 新增：加载消息配置
    }

    public void reload() {
        plugin.reloadConfig();
        config = plugin.getConfig();
        loadMessages();  // 重载时也重新加载消息
    }

    private void loadMessages() {
        File messagesFile = new File(plugin.getDataFolder(), "messages.yml");
        if (!messagesFile.exists()) {
            plugin.saveResource("messages.yml", false);
        }
        messages = YamlConfiguration.loadConfiguration(messagesFile);
    }

    public String getMessage(String key) {
        if (messages == null) {
            return "";
        }
        return messages.getString(key, "");
    }

    public String getPrefix() {
        if (messages == null) {
            return "&8[&6Forge&8] &7";
        }
        return messages.getString("prefix", "&8[&6Forge&8] &7");
    }

    public FileConfiguration getMessages() {
        return messages;
    }
}
```

#### 其他修改

- `createDefaultFiles()` 中改为创建 `forges/example_template.yml`
- 日志输出改为英文

### 5. 修改 ItemCoreForge

**文件**: `ItemCoreForge.java`

```java
// 修改前
public void reload() {
    configManager.load();
    forgeLoader.loadAll();
    layoutLoader.loadAll();
    getLogger().info("配置已重载！已加载 " + forgeLoader.getForgeCount() + " 个锻造台");
}

// 修改后
public void reload() {
    configManager.reload();
    forgeLoader.loadAll();
    layoutLoader.loadAll();
    getLogger().info("Configurations reloaded! Loaded " + forgeLoader.getForgeCount() + " forges");
}
```

### 6. 修改 plugin.yml

**文件**: `resources/plugin.yml`

```yaml
commands:
  itemcoreforge:
    description: ItemCoreForge main command
    usage: /<command> gui <forge-id> or /<command> reload
    aliases: [icf]
```

### 7. 清理 forges 文件夹

**删除的文件**:
- `forges/铁匠铺.yml`
- `forges/示例模板.yml`

**新增的文件**:
- `forges/example_template.yml`（英文示例配置）

---

## 📊 文件变更汇总

| 操作 | 文件 |
|------|------|
| 修改 | `command/ForgeCommand.java` |
| 修改 | `config/ConfigManager.java` |
| 修改 | `ItemCoreForge.java` |
| 修改 | `resources/plugin.yml` |
| 修改 | `resources/config.yml` |
| 新增 | `resources/messages.yml` |
| 新增 | `resources/forges/example_template.yml` |
| 删除 | `resources/forges/铁匠铺.yml` |
| 删除 | `resources/forges/示例模板.yml` |

---

## 📝 配置结构变化

### 修改前

```
ItemCoreForge/
├── config.yml          # 包含 settings, gui, messages
└── forges/
    ├── 铁匠铺.yml
    └── 示例模板.yml
```

### 修改后

```
ItemCoreForge/
├── config.yml          # 只包含 settings, gui
├── messages.yml        # 独立的消息配置（新增）
└── forges/
    └── example_template.yml  # 英文示例（替换中文文件）
```

---

## 🔗 相关文件

- `src/main/java/com/minemart/itemcoreforge/command/ForgeCommand.java`
- `src/main/java/com/minemart/itemcoreforge/config/ConfigManager.java`
- `src/main/java/com/minemart/itemcoreforge/ItemCoreForge.java`
- `src/main/resources/config.yml`
- `src/main/resources/messages.yml`
- `src/main/resources/plugin.yml`
- `src/main/resources/forges/example_template.yml`

---

## 🚀 使用示例

### 打开锻造站

```
/icf gui example_template
```

### 其他命令

```
/icf list          # 列出所有锻造台
/icf reload        # 重载配置
```

---

*开发者: Trae AI*  
*文档版本: 1.0.0*
